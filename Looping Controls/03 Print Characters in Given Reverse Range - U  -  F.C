// Print Characters in Given Reverse Range => U - F

#include<stdio.h>
#include<conio.h>

int main()
{
    char ch = '\0';

    for(ch = 'U' ; ch >= 'F' ; ch--)
    {
       printf("\n %c",ch);
    }

    getch();
    return 0;
}
